# This file is part of the example-skill fixture. It exists to verify that
# `lib.mkSkillFlake` does NOT copy flake plumbing into the built derivation.
# It is intentionally not a real flake and should never be evaluated by Nix.
{ /* fixture: not a real flake */ }
