---
name: beta
description: Second fixture skill in tests/example-skills-dir. Has references/ and scripts/ to exercise both copy paths.
---

# beta

Skill with `references/` and `scripts/` directories. Used to verify
`mkAllSkillsFlake` copies both into the per-skill output.
